import { EventFreqPipe } from './event-freq.pipe';

describe('EventFreqPipe', () => {
  it('create an instance', () => {
    const pipe = new EventFreqPipe();
    expect(pipe).toBeTruthy();
  });
});
